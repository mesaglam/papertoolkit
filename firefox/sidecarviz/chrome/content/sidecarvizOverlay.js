// This is our javascript


function copyToSideCar() {
	alert("Copy to SideCar XXX");
}

function startSideCar() {
	// question, can we access the HTML's DOM?
	// if we can, we can potentially talk to Flex!!! Damn... =)
	alert("Starting SideCar XXX");
}